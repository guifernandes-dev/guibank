import { BrPercentPipe } from './br-percent.pipe';

describe('BrPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new BrPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
