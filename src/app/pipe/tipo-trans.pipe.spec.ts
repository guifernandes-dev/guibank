import { TipoTransPipe } from './tipo-trans.pipe';

describe('TipoTransPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoTransPipe();
    expect(pipe).toBeTruthy();
  });
});
